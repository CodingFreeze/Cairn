# Schema — shared data contracts

> Single-writer: the orchestrator owns this file. Carries early-ticket schema forward to
> later tickets that consume it.

_(empty — populated during planning/execution)_
