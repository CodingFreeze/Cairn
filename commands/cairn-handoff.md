---
description: Emit a portable resume pack to .cairn/handoff/latest.md.
---

Use the `cairn-handoff` skill. Ensure `.cairn/` exists, run `bin/cairn handoff` to build
`.cairn/handoff/latest.md` from the board + vault, then show the user the path and contents.
Do NOT commit — writes touch files only.

$ARGUMENTS
